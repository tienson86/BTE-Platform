# Pattern philosophy

Month-command ten god defines the principal pattern. Follow and Special patterns are out of scope. Pattern modifies analytical pattern context (`pattern_score`), never `strength_score`, `season_score`, or `temperature_score`.
