# Layout Library Core

`bz_21_layout_library_core` 1.0.0 Gold. Report layouts for Report Engine.

KD-3 `package_type` is `report`. `category_id` is `layout_library`. Sprint alias: presentation.

This package does not render reports, generate HTML or CSS, paginate, or export PDF.
