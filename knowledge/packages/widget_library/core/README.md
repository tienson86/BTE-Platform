# Widget Library Core

`bz_22_widget_library_core` 1.0.0 Gold. Report widgets for Report Engine.

KD-3 `package_type` is `report`. `category_id` is `widget_library`. Sprint alias: presentation.

This package does not render markup or stylesheets, generate documents, drawings, or charts.
