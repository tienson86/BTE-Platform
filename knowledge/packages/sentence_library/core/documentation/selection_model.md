# Selection model

Engine tương lai chọn theo category, module_id, template_id, priority, confidence, visibility, và điều kiện chỗ chèn đủ dữ liệu.

Gói này không thực thi chọn lọc. Chỉ công bố metadata để chọn xác định.
