# Confidence model

`high` | `medium` | `low` trên từng câu. Không phải điểm phân tích. Câu biên và câu thiếu tin dùng medium hoặc low.

Không suy ra confidence từ chart. Không ghi đè confidence thượng nguồn.
