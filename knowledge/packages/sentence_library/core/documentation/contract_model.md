# Contract model

Input tùy chọn: các tín hiệu đã công bố từ bz_01–bz_15, đọc qua namespace analysis, decision, luck.

Output: `sentence_candidates`, `sentence_index`, `placeholder_catalog`, `template_catalog`.

Không output luận giải đã render. Không output HTML.
