# Sentence Library Core

`bz_16_sentence_library_core` 1.0.0 Gold. Thư viện câu ứng viên cố định cho Interpretation Engine.

KD-3 `package_type` là `sentence`. `category_id` là `sentence_library`.

Gói này không sinh luận giải, không render báo cáo, không dùng AI, không viết lại văn bản, không suy diễn ý nghĩa.
