# Explanation Library Core

`bz_17_explanation_library_core` 1.0.0 Gold. Khối giải thích tái sử dụng cho Interpretation Engine.

KD-3 `package_type` là `interpretation`. `category_id` là `explanation_library`.

Gói này không sinh luận giải, không tóm tắt, không viết lại, không suy diễn, không dùng AI.
