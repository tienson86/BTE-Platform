# Theme Library Core

`bz_20_theme_library_core` 1.0.0 Gold. Presentation themes for Report Engine.

KD-3 `package_type` is `report`. `category_id` is `theme_library`. Sprint alias: presentation.

This package does not render reports or generate HTML, CSS, PDF, or DOCX.
