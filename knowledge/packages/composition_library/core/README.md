# Composition and Placeholder Knowledge

`bz_19_composition_library_core` 1.0.0 Gold. Composition metadata for Interpretation Engine.

KD-3 `package_type` is `interpretation`. `category_id` is `composition_library`.

This package does not generate interpretation text, rewrite, summarize, use AI, or render reports.
