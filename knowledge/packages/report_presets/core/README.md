# Report Narrative Presets Core

`bz_23_report_presets_core` 1.0.0 Gold. Presentation presets for Report Engine.

KD-3 `package_type` is `report`. `category_id` is `report_presets`. Sprint alias: presentation.

This package does not render reports, generate markup or stylesheets, export documents, or rewrite interpretation.
