# Knowledge Indexes V2

Folder responsibilities for rebuildable Knowledge Database indexes.

| Path | Responsibility |
|------|----------------|
| `index_manifest.json` | Declares index set, sort rules, rebuild policy |
| `definitions/*.json` | Per-key index definition shells |
| `INDEX_STRATEGY.md` | Determinism and compatibility rules |
