# Graph Model

See `REASONING_FRAMEWORK.md` §2 and `reasoning_graph.schema.json`.

A package MAY ship several graphs (one per pedagogical example) plus a union index. Visualization tools SHOULD treat ids as stable keys for interactive debugging.
