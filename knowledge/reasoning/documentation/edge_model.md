# Edge Model

See `reasoning_edge.schema.json`.

Prefer `derives` for the main spine, `requires` for prerequisites, `supports`/`confirms` for parallel reinforcement, `contradicts`/`invalidates` for opposing branches, `extends` for detail that does not replace the parent.
