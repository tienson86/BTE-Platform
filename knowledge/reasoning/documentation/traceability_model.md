# Traceability Model

Traces bind a chain to:

- package identity and version
- framework version
- activated `SKC-*` (or future package prefixes)
- evidence bundle paths
- ordered decision path

Interpretation and Report layers SHOULD cite `trace_id`, not invent a parallel audit trail.
