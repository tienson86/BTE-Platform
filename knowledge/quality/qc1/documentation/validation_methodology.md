# Validation methodology

Checks are enumerated in `validation/profile.json`. Errors block QC-1 pass. Warnings (Golden Dataset, engine wiring) are recorded. Packages are never rewritten to clear a warning.
