# Limitations

Checksum verify uses byte placeholder substitution; it does not re-serialize JSON. Rule counts depend on `rules/**/*.json` shape. Evidence counts prefer `reasoning/evidence_catalog.json`. Engine scan is textual, not import-graph complete. Taxonomy denominator 34 is the KX-6D catalog size.
