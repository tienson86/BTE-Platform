# Trace Continuity

Each pipeline publishes machine-readable trace fields. Consumers record upstream pipeline ids and versions. QC-2 layer snapshots are read-only evidence that knowledge IDs exist per stage. No trace breaks in the declared graph.
