# Determinism Validation

Same declared input graph + same pipeline order → same published contract names, same trace field names, same audit flags. Replay is JSON reload of QC-3 matrices (engines not executed).
