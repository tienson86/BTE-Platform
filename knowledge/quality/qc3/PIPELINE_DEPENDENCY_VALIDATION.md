# Pipeline Dependency Validation

Dependencies are one-way. Required arrays on knowledge packages remain empty. Pipeline order is orchestration, not package `required` deps. Unknown stage ids are violations. No cycles among AX-2 → RX-1.
