# Knowledge Validation V2

Specification-only validation layer for Knowledge Database V2.

| Path | Responsibility |
|------|----------------|
| `VALIDATION_SPEC.md` | Canonical validation requirements |
| `validators/*.json` | Declarative rule catalogs (no runtime) |

Existing V1 validators in the parent folder are preserved.
