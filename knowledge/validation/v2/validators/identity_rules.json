{
  "suite": "identity",
  "version": "2.0.0",
  "mode": "specification",
  "rules": [
    {
      "code": "duplicate_id",
      "severity": "error",
      "description": "Knowledge object ids must be unique in a release corpus."
    },
    {
      "code": "immutable_id",
      "severity": "error",
      "description": "Published ids must not be reused for different objects."
    }
  ]
}
