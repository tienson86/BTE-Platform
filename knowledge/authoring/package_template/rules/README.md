# rules/

Place rule object files here for `analytical` (and similar) packages.

Start from `knowledge/authoring/templates/RULE_TEMPLATE.json`.

Leave this folder empty of production rules until real knowledge is authored through the pipeline.
