# {{PACKAGE_NAME}}

Copy this folder to a new directory named exactly `{{PACKAGE_ID}}`.

Replace all `{{PLACEHOLDERS}}` in `PACKAGE.json`, `MANIFEST.json`, and this file.

This skeleton conforms to KD-3 layout. It is not an official knowledge package until it passes the KD-4 pipeline.

## Out of scope

- Engine logic
- API or contract changes
- Golden Dataset edits
