# LEGACY PACKAGE

Status: LEGACY — retained for backward compatibility.

Do NOT delete this package.
Do NOT use for new Pack 03 runtime work.

New Pack 03 code must use:
- PackInterpretationContext
- interpreter_runtime / sentence_runtime / template_runtime /
  placeholder_runtime / explanation_runtime
- orchestration.RuntimePipeline

This package coexists with Pack 03 architecture by design.
