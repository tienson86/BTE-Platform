# Implementation Location

The Python implementation for stage **06 Combination Engine** lives in:

```text
engines/analysis_engine/combination_engine/
```

This directory (`06_combination_engine/`) remains the architecture documentation
baseline (V1.0.0 Frozen).

## Public import

```python
from engines.analysis_engine.combination_engine import (
    CombinationEngine,
    CombinationResult,
    create_default_knowledge_session,
)
```
