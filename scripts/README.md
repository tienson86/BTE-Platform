# Helper scripts (WP17)

Thin wrappers around `deployment/windows` and `deployment/linux`.

| Script | Purpose |
|--------|---------|
| `start_all.bat` / `start_all.sh` | Start API + Admin + Portal |
| `health_check.bat` / `health_check.sh` | Probe health endpoints |

Prefer calling these from the repository root.
