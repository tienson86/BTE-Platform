# BTE V1.0 customer runtime — placeholders only. Copy to a private .env.
# Full WP17 template: deployment/env/.env.example
# Docker compose: deployment/docker/.env.example

BTE_API_BASE_URL=http://127.0.0.1:8000
BTE_LOG_LEVEL=INFO
BTE_JWT_SECRET=change-me-in-production
BTE_STORAGE_BACKEND=json
BTE_DATA_DIR=applications/data
HOST=127.0.0.1
PORT=8000
PORTAL_PORT=8081
