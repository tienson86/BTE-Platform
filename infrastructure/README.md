# Infrastructure

Beta-1 production infrastructure lives under [`deployment/`](../deployment/README.md).

This folder is the scope alias for platform topology (Docker, Nginx, networks, volumes). Do not place engine or product code here.

---

END
