import type { ReactNode } from "react";

import { ShellSection } from "../shell/ShellSection";

export function FinalAssessment(): ReactNode {
  return (
    <ShellSection
      sectionId="P-S09"
      testId="final-assessment"
      title="Khuyến nghị"
      subtitle="Nhận định cuối và hướng sử dụng dãy số."
    />
  );
}
