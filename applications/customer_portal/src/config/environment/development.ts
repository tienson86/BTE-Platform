import type { EnvironmentConfig } from "./types";

export const developmentEnvironment: EnvironmentConfig = {
  name: "development",
  label: "Development",
  diagnostics: true,
  strictMode: true,
};
