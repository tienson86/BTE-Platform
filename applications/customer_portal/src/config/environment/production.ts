import type { EnvironmentConfig } from "./types";

export const productionEnvironment: EnvironmentConfig = {
  name: "production",
  label: "Production",
  diagnostics: false,
  strictMode: false,
};
