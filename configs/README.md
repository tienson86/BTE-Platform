# BTE shared configs (WP17)
#
# - deployment.ini  — human-readable service/path map
# - services.json   — machine-readable service registry
#
# Runtime secrets and overrides belong in deployment/env/*.env
