# Expected Results

Đây là kết quả chuẩn (Golden Result).

Regression Test sẽ so sánh kết quả hiện tại với các file này.

Nếu khác nhau thì Test Fail.

Chỉ cập nhật khi đã xác minh thuật toán hoặc dữ liệu mới là đúng.
